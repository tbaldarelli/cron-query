stages:
  - test
  - release

# Python tests (Linux)
test_python:
  stage: test
  image: python:3.11
  script:
    - python -m pip install --upgrade pip
    - pip install -e .[dev]
    - pytest src/python/tests/ --cov=src/python/cron_query --cov-report=term-missing
  only:
    - master
    - merge_requests

# Python tests (Windows)
# NOTE: Commented out because Windows runners are not available on this GitLab instance.
# Uncomment if Windows runners become available in the future.
# test_python_windows:
#   stage: test
#   tags:
#     - windows  # Requires Windows runner
#   script:
#     - python -m pip install --upgrade pip
#     - pip install -e .[dev]
#     - pytest src/python/tests/ --cov=src/python/cron_query --cov-report=term-missing
#   allow_failure: true  # Optional - Windows runners may not be available
#   only:
#     - master
#     - merge_requests

# Groovy/Gradle tests (Linux)
test_groovy:
  stage: test
  image: eclipse-temurin:21-jdk
  script:
    - ./gradlew test
  artifacts:
    reports:
      junit: build/test-results/test/TEST-*.xml
    paths:
      - build/reports/tests/test/
  only:
    - master
    - merge_requests

# Groovy/Gradle tests (Windows)
# NOTE: Commented out because Windows runners are not available on this GitLab instance.
# Uncomment if Windows runners become available in the future.
# test_groovy_windows:
#   stage: test
#   tags:
#     - windows
#   script:
#     - ./gradlew.bat test
#   artifacts:
#     paths:
#       - build/reports/tests/test/
#   allow_failure: true  # Optional - Windows runners may not be available
#   only:
#     - master
#     - merge_requests

# Build release artifacts
build_release:
  stage: release
  image: python:3.11
  before_script:
    - apt-get update && apt-get install -y openjdk-21-jdk
  script:
    - python -m pip install --upgrade pip build twine
    - python -m build
    - ./gradlew build -x test
    - |
      VERSION=${CI_COMMIT_TAG#v}
      echo "Release version: $VERSION"
  artifacts:
    paths:
      - dist/*.whl
      - dist/*.tar.gz
      - build/libs/*.jar
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_TAG =~ /^v/

# Create GitLab release
create_release:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  needs:
    - job: build_release
      artifacts: true
  script:
    - echo "Creating release for $CI_COMMIT_TAG"
  rules:
    - if: $CI_COMMIT_TAG =~ /^v/
  release:
    tag_name: $CI_COMMIT_TAG
    description: "See CHANGELOG.md for details."
    assets:
      links:
        - name: "Python wheel"
          url: "$CI_PROJECT_URL/-/jobs/artifacts/$CI_COMMIT_TAG/browse/dist?job=build_release"
        - name: "Groovy JAR"
          url: "$CI_PROJECT_URL/-/jobs/artifacts/$CI_COMMIT_TAG/browse/build/libs?job=build_release"
